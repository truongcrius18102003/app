name: app_1
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations: # Thêm dependency cho flutter_localizations
    sdk: flutter
  intl: 0.18.1 # Thêm dependency cho intl package

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
  assets:
    - assets/coffee.jpg
    - assets/tea.png
    - assets/milk.png
    - assets/juice.png
